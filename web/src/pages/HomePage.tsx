export function HomePage() {
  return (
    <section>
      <h2 className="text-xl font-semibold text-slate-800">Estructura lista</h2>
      <p className="mt-2 text-slate-600">
        La base del proyecto (Fase 1) está configurada. Las funcionalidades de
        negocio (productos, proveedores, stock, pedidos, dashboard) se
        implementarán en la Fase 2.
      </p>
    </section>
  )
}