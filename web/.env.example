# Copia este archivo como `.env.local` en el directorio `web/`.
# Solo valores públicos (ANON key). NUNCA pongas la service role key aquí.

VITE_API_URL=http://localhost:4000/api
VITE_SUPABASE_URL=https://XXXXXXXX.supabase.co
VITE_SUPABASE_ANON_KEY=