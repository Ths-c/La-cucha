{
  "installCommand": "npm --prefix web install",
  "buildCommand": "node -v && npm -v && npm --prefix web run build",
  "outputDirectory": "web/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}